import '../../styles/annotate-ui/index.scss';

/**
 * Main component for the annotation app. this will be injected into the router.
 */
export function AnnotateApp() {
  return <div>AnnotateApp</div>;
}
