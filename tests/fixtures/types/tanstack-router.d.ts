// Minimal type stubs for @tanstack/react-router test fixtures
// These fixtures are for route discovery testing only and don't need full types

declare module '@tanstack/react-router' {
  export function createFileRoute(path: string): unknown;
}